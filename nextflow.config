process {
    executor = 'slurm'

    cpus = 8
    memory = '32 GB'
    time = '2-23:00:00'

    beforeScript = "module load apptainer"
    clusterOptions = '--qos=medium'
}


apptainer {
    enabled = true
    autoMounts = true
    pullTimeout = '20 min'
}